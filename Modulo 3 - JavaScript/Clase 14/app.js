//Arrow functions

const saludar = (nombre, apellido) => {
    return `Hola! Mi nombre es ${nombre} ${apellido}`;
}

//Otra forma para cuando son de una sola linea
const saludar2 = (nombre) => `Hola ${nombre}`;