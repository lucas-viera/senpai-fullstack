const operation = require("./operations");

const resultSuma = operation.sumar(2, 8);
const resultResta = operation.restar(2, 8);
const resultMult = operation.multiplicar(2, 8);

console.log(resultSuma);
console.log(resultResta);
console.log(resultMult);
